services:
  - type: web
    name: cosmos-explorer-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: CLIENT_URL
        sync: false
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 4000
